# UI 统一化改进计划

## 📊 现状分析

### 当前设计系统（已有）
- ✅ 颜色变量完整（dark/light 主题）
- ✅ 圆角变量（radius-sm/md/lg/xl）
- ✅ 过渡动画变量
- ✅ 阴影变量
- ✅ 玻璃拟态变量

### 需要改进的问题
1. **缺少 Spacing 系统** - 无统一间距规范
2. **按钮样式分散** - 多处定义按钮样式
3. **大量内联样式** - `style={{ }}` 过多
4. **图标不统一** - 混用 emoji 和 Lucide
5. **CSS 文件过大** - 9143 行，有重复

---

## 🎯 改进方案

### Phase 1: 基础设计系统扩展
1. 添加 Spacing 系统变量
2. 添加字体大小系统变量
3. 统一按钮组件样式

### Phase 2: 组件统一化
1. 创建统一 Button 组件
2. 创建统一 Card 组件
3. 创建统一 Modal 组件
4. 创建统一 Input 组件

### Phase 3: 清理和优化
1. 移除重复 CSS
2. 将内联样式转为 class
3. 统一图标使用 Lucide

---

## 🛠 Phase 1 详细实施

### 1.1 添加 Spacing 系统
```css
:root {
  --space-0: 0;
  --space-1: 4px;     /* xs */
  --space-2: 8px;     /* sm */
  --space-3: 12px;    /* md */
  --space-4: 16px;    /* lg */
  --space-5: 20px;
  --space-6: 24px;    /* xl */
  --space-8: 32px;    /* 2xl */
  --space-10: 40px;
  --space-12: 48px;   /* 3xl */
  --space-16: 64px;
}
```

### 1.2 添加字体大小系统
```css
:root {
  --text-xs: 0.75rem;    /* 12px */
  --text-sm: 0.875rem;   /* 14px */
  --text-base: 1rem;     /* 16px */
  --text-lg: 1.125rem;   /* 18px */
  --text-xl: 1.25rem;    /* 20px */
  --text-2xl: 1.5rem;    /* 24px */
  --text-3xl: 1.875rem;  /* 30px */
}
```

### 1.3 统一按钮类名
```css
/* 主按钮 */
.btn-primary { }
.btn-secondary { }
.btn-cta { }
.btn-ghost { }
.btn-danger { }

/* 按钮尺寸 */
.btn-sm { }
.btn-md { }
.btn-lg { }

/* 按钮状态 */
.btn:disabled { }
.btn:hover { }
```

---

## 📅 执行顺序

1. [x] Git 备份完成
2. [ ] 添加 Spacing 和字体变量到 :root
3. [ ] 添加统一按钮样式
4. [ ] 添加工具类（spacing utilities）
5. [ ] 测试构建
6. [ ] 逐步替换内联样式
