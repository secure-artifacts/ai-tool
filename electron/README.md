# SheetMind 桌面版

这是 SheetMind 数据分析工具的 Electron 桌面版，支持 **GB 级大数据本地缓存**。

## 优势

| 功能 | 网页版 | 桌面版 |
|------|--------|--------|
| 数据缓存大小 | ~3MB | **无限制** |
| 离线访问 | ❌ | ✅ |
| 加载速度 | 每次从网络加载 | 优先使用本地缓存 |
| 数据同步 | Firestore | Firestore（同步） |

## 使用方法

### 1. 开发模式

```bash
# 终端 1: 启动 Vite 开发服务器
cd /Volumes/jw/代码/🪄\ AI\ 创作工具包/ai-创作工具包-正式版
npm run dev

# 终端 2: 启动 Electron
cd electron
npm run start:dev
```

### 2. 打包发布

```bash
# 先构建网页版
cd ..
npm run build

# 打包 macOS 版
cd electron
npm run build:mac

# 打包 Windows 版（需要在 Windows 上运行，或使用 CI）
npm run build:win
```

打包后的应用在 `dist-electron/` 目录。

## 数据缓存位置

- **macOS**: `~/Library/Application Support/sheetmind-desktop/sheetmind-cache/`
- **Windows**: `%APPDATA%/sheetmind-desktop/sheetmind-cache/`

## 技术架构

```
┌──────────────────────────────────────────┐
│             SheetMind React App          │
│         （与网页版完全相同的代码）          │
└──────────────────┬───────────────────────┘
                   │
         ┌─────────┴─────────┐
         ↓                   ↓
┌─────────────────┐  ┌─────────────────┐
│ 网页环境         │  │ Electron 环境    │
│ localStorage    │  │ 本地文件系统      │
│ (限制 5MB)      │  │ (无限制)         │
└─────────────────┘  └─────────────────┘
```

## 扩展为完整工具包

如需将整个 AI 创作工具包打包为桌面版，只需修改 `main.js` 中的加载路径即可。
